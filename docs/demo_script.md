# Demo Script

1. Start the FastAPI backend: uvicorn backend.app.main:app --reload.
2. Build + launch the Electron app: cd desktop && npm install && npm run dev.
3. Inside the UI, upload a sample media file and pick an operational context.
4. Highlight the JSON-like response rendered on screen (dummy inference + reasoning).
5. Explain how EfficientNetV2 + Ollama will replace dummy logic for a fully local AI stack.

